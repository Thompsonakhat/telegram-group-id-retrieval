export const BOT_PROFILE = [
  "Purpose: show the current Telegram chat ID so users can configure integrations and identify private, group, or supergroup IDs.",
  "Public commands: /start for onboarding, /help for usage guidance, /id to return the current chat ID.",
  "Key rules: Telegram only, no AI, no database, no admin features. In groups, users should run /id inside the target chat. Group privacy settings or permissions can affect whether the bot receives commands.",
].join(" ");
